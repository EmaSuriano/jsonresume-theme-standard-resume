{{#resume.basics}}
  <h1>{{name}}</h1>
  <p>{{label}}</p>

  {{#if location}}
    <p class="sub-headline">{{location.city}}, {{location.countryCode}}</p>
  {{/if}}

  {{#if summary}}
    <p class="sub-headline">{{summary}}</p>
  {{/if}}

{{/resume.basics}}